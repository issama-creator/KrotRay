# Как работает система ограничения устройств - Техническое объяснение

## ✅ Техническая корректность

### 1. **Stats API - Правильно**
- Используется `StatsService.GetStats` с именем `"user>>>UUID>>>connections"`
- Это стандартный способ получения статистики соединений в Xray
- Формат имени статистики соответствует документации Xray

### 2. **Enable/Disable - Правильно**
- **Disable**: `RemoveUserOperation` - удаляет пользователя из inbound Xray
- **Enable**: `AddUserOperation` - добавляет пользователя обратно в inbound
- UUID **не меняется** - используется тот же UUID при AddUser
- Xray **не перезапускается** - изменения применяются через gRPC API в реальном времени

### 3. **Worker логика - Правильно**
- Проверка каждые 60 секунд
- `violation_count >= 2` перед отключением (защита от ложных срабатываний)
- Автоматическое включение при восстановлении нормы
- Правильная обработка ошибок с rollback

## 📋 Пример работы (пошагово)

### Сценарий: Пользователь купил подписку на 1 устройство

**Шаг 1: Покупка подписки**
```
Пользователь выбирает: 1 устройство, тариф "1 месяц"
→ UI отправляет: {tariff: "1m", devices: 1, price: 100}
→ Backend создает Payment: devices=1
→ Backend создает Subscription: allowed_devices=1, uuid="abc-123..."
→ Xray: AddUser(uuid="abc-123...", email="user_42")
→ Статус: active, disabled_by_limit=false, violation_count=0
```

**Шаг 2: Подключение с телефона**
```
Телефон подключается к VPN с UUID="abc-123..."
→ Xray Stats API: user>>>abc-123...>>>connections = 1
→ Worker проверяет: connections=1, allowed_devices=1
→ 1 <= 1 ✅ В норме
→ Статус: active, violation_count=0
```

**Шаг 3: Пользователь дал ключ другу (превышение лимита)**
```
Друг подключается к VPN с тем же UUID="abc-123..."
→ Xray Stats API: user>>>abc-123...>>>connections = 2
→ Worker проверяет (через 60 сек): connections=2, allowed_devices=1
→ 2 > 1 ❌ Превышение!
→ violation_count = 0 + 1 = 1
→ Статус: active, violation_count=1 (еще не отключен)
```

**Шаг 4: Вторая проверка (через 60 сек)**
```
Xray Stats API: user>>>abc-123...>>>connections = 2
→ Worker проверяет: connections=2, allowed_devices=1
→ 2 > 1 ❌ Превышение!
→ violation_count = 1 + 1 = 2
→ violation_count >= 2 ✅ Отключаем!
→ Xray: RemoveUser(email="user_42") - удаляет пользователя из inbound
→ disabled_by_limit = True
→ Статус: active, disabled_by_limit=true, violation_count=2
→ Результат: Интернет пропал у обоих (телефон + друг)
```

**Шаг 5: Друг отключился**
```
Друг отключается от VPN
→ Xray Stats API: user>>>abc-123...>>>connections = 0 (пользователь удален, соединений нет)
→ Worker проверяет (через 60 сек): connections=0, allowed_devices=1
→ 0 <= 1 ✅ В норме!
→ violation_count = 0 (сброс)
→ disabled_by_limit = True → нужно включить!
→ Xray: AddUser(uuid="abc-123...", email="user_42") - добавляет пользователя обратно
→ disabled_by_limit = False
→ Статус: active, disabled_by_limit=false, violation_count=0
→ Результат: Интернет вернулся у пользователя
```

## 🔍 Детали реализации

### Stats API формат
```python
stats_name = f"user>>>{uuid}>>>connections"
# Пример: "user>>>abc-123-def-456>>>connections"
```

Xray автоматически создает эту статистику для каждого пользователя при включенном Stats API.

### Enable/Disable механизм
```python
# Disable (отключение)
RemoveUserOperation(email="user_42")
# → Пользователь удаляется из inbound, соединения разрываются

# Enable (включение)
AddUserOperation(user=User(uuid="abc-123...", email="user_42"))
# → Пользователь добавляется обратно с тем же UUID
```

### Worker цикл
```python
while True:
    1. SELECT активные подписки (status='active', expires_at > NOW(), uuid IS NOT NULL)
    2. Для каждой подписки:
       a. get_connections(uuid) через Stats API
       b. Если connections > allowed_devices:
          - violation_count += 1
          - Если violation_count >= 2: disable_user()
       c. Если connections <= allowed_devices:
          - violation_count = 0
          - Если disabled_by_limit: enable_user()
    3. sleep(60 секунд)
```

## ⚠️ Важные моменты

### 1. Stats API должен быть включен
Без этого `get_connections()` всегда вернет 0, и система не будет работать.

### 2. Задержка обнаружения
- Минимум: 60 секунд (первая проверка)
- Максимум: 120 секунд (2 проверки для отключения)
- Это нормально для production - защищает от кратковременных скачков

### 3. UUID не меняется
При disable/enable UUID остается прежним, пользователю не нужно менять конфигурацию клиента.

### 4. Xray не перезапускается
Все изменения через gRPC API в реальном времени, без downtime.

## 🧪 Тестирование

### Тест 1: Превышение лимита
1. Создать подписку с `allowed_devices=1`
2. Подключить 2 устройства одновременно
3. Подождать ~2 минуты
4. Ожидаемый результат: оба устройства потеряют интернет

### Тест 2: Восстановление
1. После теста 1, отключить одно устройство
2. Подождать ~1 минуту
3. Ожидаемый результат: оставшееся устройство снова получит интернет

### Тест 3: Ложное срабатывание
1. Подключить 2 устройства
2. Отключить одно через 30 секунд (до второй проверки)
3. Ожидаемый результат: пользователь НЕ будет отключен (violation_count сбросится)

## 📊 Логи для отладки

```bash
# Логи worker'а
sudo journalctl -u krotray-device-limiter -f

# Пример логов:
[INFO] Проверка 5 активных подписок
[WARNING] Subscription 42: превышение лимита! connections=2 > allowed=1 violation_count=1
[WARNING] Subscription 42: превышение лимита! connections=2 > allowed=1 violation_count=2
[INFO] Subscription 42: пользователь отключен из-за превышения лимита устройств
[INFO] Subscription 42: соединения в норме, violation_count сброшен
[INFO] Subscription 42: пользователь автоматически включен (соединения в норме)
```

## ✅ Итоговая оценка

**Технически корректно:** ✅
- Правильное использование Xray Stats API
- Правильное использование gRPC для enable/disable
- Корректная логика worker'а с защитой от ложных срабатываний
- Правильная обработка ошибок

**Готово к production:** ✅
- После включения Stats API в конфиге Xray
- После применения миграций БД
- После запуска worker'а через systemd
