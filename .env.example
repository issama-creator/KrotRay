BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
MINI_APP_URL=https://krot-ray.vercel.app
API_URL=http://localhost:8000
# Cloaking dynamic UI
# Telegram deep-link base: итог = <base><uid>
CLOAK_TELEGRAM_DEEP_LINK_BASE=tg://resolve?domain=krotraybot&start=pay_
# Публичное имя бота для текстов (без @). Полная фраза «продлить подписку…» — см. CLOAK_TELEGRAM_RENEWAL_HINT_RU.
CLOAK_TELEGRAM_BOT_PUBLIC=krotraybot
# 0 = только открыть бота (https://t.me/… и tg:// без ?start=); оплата по telegram_id из бота. 1 = в start передаётся pay_<uid> для корреляции с приложением до оплаты.
CLOAK_TELEGRAM_INCLUDE_START_PAYLOAD=0
# Необязательно: переопределить строку целиком для texts.telegram_renewal_hint и telegram_cta (иначе «Продлите подписку в Telegram — @…»).
# CLOAK_TELEGRAM_RENEWAL_HINT_RU=Продлите подписку в Telegram — @krotraybot
# Public URL of technical white page (shown in SAFE mode text)
CLOAK_WHITE_PAGE_URL=https://krotray.ru/api/pay
# Debug only: ignore emulator sid check in /api/config mode detection (1=on, 0=off)
CLOAK_IGNORE_EMULATOR=0
# QA only: добавить к отображаемым дням триала (баннер/modal), реальный trial_until_ts не меняется. Например -1 = показать на 1 день меньше.
CLOAK_TRIAL_DAYS_DISPLAY_OFFSET=0
# Пер-user сильнее env: redis-cli SET "cloak:trial_display_off:<device_id>" -1
# Control plane (Flutter): в проде — PostgreSQL. Локально часто sqlite:///./krotray.db
DATABASE_URL=sqlite:///./krotray.db
# DATABASE_URL=postgresql://user:password@localhost:5432/krotray
# ЮKassa (Итерация 5)
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=
# Webhook в ЛК ЮKassa: https://<your-domain>/api/payments/webhook
# Только key-factory (без Xray/Subscription/cp в webhook): MINIMAL_PAYMENT_WEBHOOK=1
MINIMAL_PAYMENT_WEBHOOK=0
# Куда вернуть пользователя после оплаты. По умолчанию — в бота (личный кабинет). Можно указать свой URL.
PAYMENT_RETURN_URL=https://t.me/krotraybot

# Xray gRPC (Итерация 6.1): тег inbound для AddUser
XRAY_INBOUND_TAG=vless-in

# Первый сервер (для скрипта scripts/add_first_server.py)
# Xray на отдельном сервере — укажи его IP и gRPC-порт из конфига Xray (API)
# Пример: Xray на 103.137.251.165, бот на krotray.ru — host=103.137.251.165
XRAY_SERVER_NAME=Main
XRAY_SERVER_HOST=103.137.251.165
XRAY_GRPC_PORT=8080
XRAY_MAX_USERS=100

# VLESS-ссылка (6.2): в шаблоне подставь {uuid} — бэкенд подставит UUID пользователя
VLESS_URL_TEMPLATE=vless://{uuid}@103.137.251.165:443?encryption=none&flow=xtls-rprx-vision&security=reality&sni=www.apple.com&fp=chrome&pbk=OgH3-UkoJbWUhmz_bTPmUbRQbAOyRhIgQ841KI4C42w&sid=568d2499&type=tcp#KrotRay

# Edge top cache (Redis)
# EDGE_REDIS_URL=redis://127.0.0.1:6379/0
EDGE_TOP_CACHE_REFRESH_SEC=10
EDGE_TOP_CACHE_TTL_SEC=30
EDGE_TOP_CACHE_KEY=edge:top:candidates:v1

# Сколько устройств (platform+device_stable_id) можно привязать к одному access key (GET /servers?key=...)
ACCESS_KEY_MAX_DEVICES=3

# Minimal key factory runtime (балансировка в Redis)
# REDIS_URL=redis://127.0.0.1:6379/0
# Инициализация каталога: py -3 scripts/seed_redis_key_factory.py --from-json servers.example.json
# Отдельный процесс: python -m workers.main — только TCP health → Redis server:* status
HEALTH_CHECK_INTERVAL_SEC=45
HEALTH_CHECK_PORT=443
HEALTH_CHECK_TIMEOUT_SEC=1.5